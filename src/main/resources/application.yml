spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java_matrix?serverTimezone=Asia/Seoul
    username: root

  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
